services:
  api:
    build:
      context: .
      dockerfile: apps/kitchen_mate/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    restart: unless-stopped
